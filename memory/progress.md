# Build Progress Log — append-only

> One line per meaningful slice landing or status change: `- [UTC time] who — slice — state — test result`.
> Append-only; never rewrite past lines. Run `./scripts/sync.sh` after appending.
> States: started | branch-green | merged-to-main | blocked. Detailed task states live in team-board.md.

- [2026-06-13T10:15:00Z] Plan — parallel build plan written (plan-owner.md, plan-v.md, parallel-plan.md). Streams: V=foundation+data+back-office+forecast-engine, Owner=sales-front+AI+server/deploy. Awaiting V WAVE 0 foundation.
- [2026-06-13T11:00:00Z] Owner — server prep DONE. Frankfurt has Docker 29.3.1+Compose v5.1.1+git; /opt/hmd-crm created; ports 3000/5432 free. WARNING: shared box with live "jianglai" project (don't touch; port 80/8000/3307 taken). RAM tight (~0.5Gi free +2.1Gi swap) → build-OOM risk. Constraints written to memory/deploy-notes.md for V's docker-compose (project name hmd-crm, app:3000, postgres internal, standalone build).
- [2026-06-13T11:35:00Z] Owner — Featherless smoke test PASSED (key from PromptFinance/.env). Qwen/Qwen2.5-7B-Instruct = 200, ~1.84s, clean JSON in content → KEPT as default chat model. GLM-4.6 = 200 but returns answer in `reasoning` field w/ empty content (reasoning model) → avoid for JSON; client now reads content||reasoning as a safety net. AI provider fully validated; only TODO = copy FEATHERLESS_API_KEY into our repo .env at deploy.
- [2026-06-13T11:36:00Z] V — WAVE 0 foundation — merged-to-main (fb3aaab) — Next.js 15 scaffold + full Prisma schema (14 entities) + rich seed (8 accounts/18 deals/3-yr forecast/12 cases/full catalog/offers in every approval state) + docker-compose/Dockerfile/run.sh + demo role-switch shell + lib/* helpers. Test: tsc --noEmit clean + next build GREEN; runtime seed verify pending local Postgres role provisioning. **Owner UNBLOCKED — branch off main now.** Integration contract = lib/session(currentUser/currentRole), lib/activity(createActivityEvent), lib/notify(notify), lib/forecast(weightedRevenue + quarter aggregation). NOTE for deploy: docker-compose still exposes postgres:5432 + uses Azure OpenAI env names — needs aligning to deploy-notes (postgres internal, project name hmd-crm) + Featherless env before Frankfurt.
- [2026-06-13T11:42:00Z] V — foundation deploy-align — merged-to-main — RESOLVED the deploy NOTE above: .env.example now Featherless (FEATHERLESS_API_KEY / FEATHERLESS_MODEL=Qwen/Qwen2.5-7B-Instruct / FEATHERLESS_BASE_URL, dropped AZURE_OPENAI; added POSTGRES_*); docker-compose.yml per deploy-notes (project name hmd-crm, app:3000, postgres INTERNAL/not published, postgres:16-alpine, env-driven, app DATABASE_URL→host "db"). lib/* helper signatures reconfirmed == parallel-plan. tsc + next build still GREEN. RUNTIME (docker compose up → seed + render) NOT run locally — Docker is NOT installed on V's machine (WSL2 is present); container verify deferred to Frankfurt (has Docker 29.3.1 + Compose) or a local Docker install. Owner remains unblocked.
- [2026-06-13T12:30:00Z] Owner — RUNTIME FOUNDATION VERIFIED (the part V couldn't): prisma db push OK, seed wrote 8 accounts/18 deals/156 forecast rows/12 cases/5 offers OK, dev server renders /, /role-switch 200. Featherless key validated live (Qwen2.5-7B ~1.8s).
- [2026-06-13T12:30:00Z] Owner — SA-O1 DONE & merged to main (ab46225): Account 360 page (/accounts/[id]: deals+cases together, offers, timeline, notes+add-note action, contacts, NBA panel) + Rep dashboard (/rep: my accounts, at-risk deals, pipeline by stage, offers in approval, activity) + rule-based Next Best Action panel (live now; SA-O4 adds LLM on top). tsc clean, both pages 200 with seed data.
- [2026-06-13T12:30:00Z] Owner -> V FINDING: run.sh local path is broken — docker-compose `db` uses `expose` (internal only, NOT published to host), so `npm run dev` on host can't reach localhost:5432. For LOCAL dev the db must be published (e.g. a docker-compose.override.yml mapping 5432, or run.sh starts a published dev db). Frankfurt deploy is fine (full app-in-compose reaches db by service name). I verified using a standalone published postgres. Please fix the local-dev path, V.
- [2026-06-13T13:00:00Z] Owner — SA-O2 DONE (main): /deals/new (channel→stage live filter, reseller hides Contract negotiation, 4-quarter year-1 forecast input) + createDeal action (writes 12 DealForecastPeriod rows: y1 entered, y2/y3 projected ×1.2/1.35, device/service separate, weighted by stage prob) + /deals/[id] detail (3-yr forecast table, move-stage re-weights rows). tsc clean; pages 200; nested deal+12-row write verified + cascade cleanup. Account "New deal" button now works.
- [2026-06-13T13:30:00Z] Owner — SA-O3 DONE (main): /offers/new offer builder (active catalog products+services, qty, live total, discount% + justification REQUIRED when >0) + createOffer action + /offers/[id] detail (line items w/ price snapshots, discount+justification, status, approval history). Verified: discounted submit -> Offer.status=PENDING_SM, locked, Approval{SALES_MANAGER,PENDING}, all SMs notified; no-discount submit -> APPROVED. tsc clean, pages 200, write path + cascade verified.
- [2026-06-13T13:30:00Z] HANDOFF CONTRACT for V (SA-V2 approval queues) — Owner's submit leaves a discounted offer at: Offer.status=PENDING_SM, locked=true, one Approval{step:SALES_MANAGER,status:PENDING}, SMs notified. V's queue actions continue: SM approve -> Approval(SM)=APPROVED + Offer.status=PENDING_FINANCE + create Approval{FINANCE,PENDING} + notify FINANCE; Finance approve -> Approval(FINANCE)=APPROVED + Offer.status=APPROVED + notify rep; reject(any) -> Approval=REJECTED + Offer.status=REJECTED + locked=false + notify rep. (Full contract in app/offers/actions.ts header.)
