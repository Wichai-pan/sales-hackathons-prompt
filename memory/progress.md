# Build Progress Log — append-only

> One line per meaningful slice landing or status change: `- [UTC time] who — slice — state — test result`.
> Append-only; never rewrite past lines. Run `./scripts/sync.sh` after appending.
> States: started | branch-green | merged-to-main | blocked. Detailed task states live in team-board.md.

- [2026-06-13T10:15:00Z] Plan — parallel build plan written (plan-owner.md, plan-v.md, parallel-plan.md). Streams: V=foundation+data+back-office+forecast-engine, Owner=sales-front+AI+server/deploy. Awaiting V WAVE 0 foundation.
- [2026-06-13T11:00:00Z] Owner — server prep DONE. Frankfurt has Docker 29.3.1+Compose v5.1.1+git; /opt/hmd-crm created; ports 3000/5432 free. WARNING: shared box with live "jianglai" project (don't touch; port 80/8000/3307 taken). RAM tight (~0.5Gi free +2.1Gi swap) → build-OOM risk. Constraints written to memory/deploy-notes.md for V's docker-compose (project name hmd-crm, app:3000, postgres internal, standalone build).
- [2026-06-13T11:35:00Z] Owner — Featherless smoke test PASSED (key from PromptFinance/.env). Qwen/Qwen2.5-7B-Instruct = 200, ~1.84s, clean JSON in content → KEPT as default chat model. GLM-4.6 = 200 but returns answer in `reasoning` field w/ empty content (reasoning model) → avoid for JSON; client now reads content||reasoning as a safety net. AI provider fully validated; only TODO = copy FEATHERLESS_API_KEY into our repo .env at deploy.
- [2026-06-13T11:36:00Z] V — WAVE 0 foundation — merged-to-main (fb3aaab) — Next.js 15 scaffold + full Prisma schema (14 entities) + rich seed (8 accounts/18 deals/3-yr forecast/12 cases/full catalog/offers in every approval state) + docker-compose/Dockerfile/run.sh + demo role-switch shell + lib/* helpers. Test: tsc --noEmit clean + next build GREEN; runtime seed verify pending local Postgres role provisioning. **Owner UNBLOCKED — branch off main now.** Integration contract = lib/session(currentUser/currentRole), lib/activity(createActivityEvent), lib/notify(notify), lib/forecast(weightedRevenue + quarter aggregation). NOTE for deploy: docker-compose still exposes postgres:5432 + uses Azure OpenAI env names — needs aligning to deploy-notes (postgres internal, project name hmd-crm) + Featherless env before Frankfurt.
- [2026-06-13T11:42:00Z] V — foundation deploy-align — merged-to-main — RESOLVED the deploy NOTE above: .env.example now Featherless (FEATHERLESS_API_KEY / FEATHERLESS_MODEL=Qwen/Qwen2.5-7B-Instruct / FEATHERLESS_BASE_URL, dropped AZURE_OPENAI; added POSTGRES_*); docker-compose.yml per deploy-notes (project name hmd-crm, app:3000, postgres INTERNAL/not published, postgres:16-alpine, env-driven, app DATABASE_URL→host "db"). lib/* helper signatures reconfirmed == parallel-plan. tsc + next build still GREEN. RUNTIME (docker compose up → seed + render) NOT run locally — Docker is NOT installed on V's machine (WSL2 is present); container verify deferred to Frankfurt (has Docker 29.3.1 + Compose) or a local Docker install. Owner remains unblocked.
- [2026-06-13T12:08:00Z] V — WAVE 1 ALL FOUR SLICES landed on main — merged-to-main — built in parallel (4 subagents): SA-V1 Catalog (/catalog + lib/catalog.ts, retire logic, activeProducts/Services feed for Owner's offer builder), SA-V2 Offer approval (/approvals + /approvals/[offerId] + lib/approval.ts STATE MACHINE = submitForApproval/smApprove/smReject/financeApprove/financeReject — discount-needs-justification, lock-while-pending, HARD Finance-after-SM guard, reject-unlocks, $transaction, notify+activity on every step), SA-V3 TAM cases (/tam + /cases/[id] + lib/cases.ts — notes w/ internal tier, close sets closedAt, escalate), SA-V4 Manager+Finance+forecast (/manager + /finance + /reports + lib/reporting.ts — 3-yr quarterly forecast device/service SEPARATE, weighted pipeline, stalled>14d, past-close, quarter/half/year toggle, reassign, close rate). nav.tsx wired with role-aware links. Test: tsc --noEmit clean + next build GREEN (all 11 routes compiled, dynamic). lib/approval.ts hand-reviewed = correct. **Contracts for Owner:** offer builder calls lib/approval.submitForApproval(offerId) + uses lib/catalog.activeProducts()/activeServices(). RUNTIME seed+click verify still pending Docker (Frankfurt). main stays build-demoable.
- [2026-06-13T12:30:00Z] Owner — RUNTIME FOUNDATION VERIFIED (the part V couldn't): prisma db push OK, seed wrote 8 accounts/18 deals/156 forecast rows/12 cases/5 offers OK, dev server renders /, /role-switch 200. Featherless key validated live (Qwen2.5-7B ~1.8s).
- [2026-06-13T12:30:00Z] Owner — SA-O1 DONE & merged to main (ab46225): Account 360 page (/accounts/[id]: deals+cases together, offers, timeline, notes+add-note action, contacts, NBA panel) + Rep dashboard (/rep: my accounts, at-risk deals, pipeline by stage, offers in approval, activity) + rule-based Next Best Action panel (live now; SA-O4 adds LLM on top). tsc clean, both pages 200 with seed data.
- [2026-06-13T12:30:00Z] Owner -> V FINDING: run.sh local path is broken — docker-compose `db` uses `expose` (internal only, NOT published to host), so `npm run dev` on host can't reach localhost:5432. For LOCAL dev the db must be published (e.g. a docker-compose.override.yml mapping 5432, or run.sh starts a published dev db). Frankfurt deploy is fine (full app-in-compose reaches db by service name). I verified using a standalone published postgres. Please fix the local-dev path, V.
- [2026-06-13T13:00:00Z] Owner — SA-O2 DONE (main): /deals/new (channel→stage live filter, reseller hides Contract negotiation, 4-quarter year-1 forecast input) + createDeal action (writes 12 DealForecastPeriod rows: y1 entered, y2/y3 projected ×1.2/1.35, device/service separate, weighted by stage prob) + /deals/[id] detail (3-yr forecast table, move-stage re-weights rows). tsc clean; pages 200; nested deal+12-row write verified + cascade cleanup. Account "New deal" button now works.
- [2026-06-13T13:30:00Z] Owner — SA-O3 DONE (main): /offers/new offer builder (active catalog products+services, qty, live total, discount% + justification REQUIRED when >0) + createOffer action + /offers/[id] detail (line items w/ price snapshots, discount+justification, status, approval history). Verified: discounted submit -> Offer.status=PENDING_SM, locked, Approval{SALES_MANAGER,PENDING}, all SMs notified; no-discount submit -> APPROVED. tsc clean, pages 200, write path + cascade verified.
- [2026-06-13T13:30:00Z] HANDOFF CONTRACT for V (SA-V2 approval queues) — Owner's submit leaves a discounted offer at: Offer.status=PENDING_SM, locked=true, one Approval{step:SALES_MANAGER,status:PENDING}, SMs notified. V's queue actions continue: SM approve -> Approval(SM)=APPROVED + Offer.status=PENDING_FINANCE + create Approval{FINANCE,PENDING} + notify FINANCE; Finance approve -> Approval(FINANCE)=APPROVED + Offer.status=APPROVED + notify rep; reject(any) -> Approval=REJECTED + Offer.status=REJECTED + locked=false + notify rep. (Full contract in app/offers/actions.ts header.)
- [2026-06-13T13:35:00Z] V — FIX for Owner's local-dev finding + WAVE 1 verified on main. (1) Added docker-compose.dev.yml (PUBLISHED postgres on host:5432); run.sh/run.ps1 now start it before `next dev`. Main docker-compose.yml stays postgres-INTERNAL (Frankfurt full-stack unaffected). Host `next dev` -> localhost:5432 works again. (2) WAVE 1 SA-V1..V4 merged to main; COMBINED tree (Owner SA-O1/O2/O3 + V WAVE 1 = 17 routes) passes tsc + next build GREEN. Owner: approval queues are live at /approvals — your offer submit already lands PENDING_SM + notifies SM, so SM->Finance approve/reject now works end-to-end via lib/approval.ts.
- [2026-06-13T14:30:00Z] Owner — SA-O4 core DONE (main): lib/ai/{client,intake,nba} on Featherless (runtime env read; exact-shape prompt; content||reasoning). AI-assisted intake (rep dashboard: paste→/api/ai/intake→draft preview→applyIntake writes account+contact+deal+case+note) VERIFIED source:ai (extracted contact+deal+CRITICAL case+task). NBA panel on account page via LLM+rules fallback, Suspense-streamed (page paints instant). Removed debug diag route.
- [2026-06-13T14:30:00Z] INTEGRATION VERIFIED — full app (Owner SA-O1..O4 + V SA-V2..V4) tsc CLEAN; all 14 routes 200 by role (/rep /accounts /deals /offers + /tam /manager /finance /approvals /catalog /reports). Offer workflow cross-person chain tested end-to-end: DRAFT→PENDING_SM→PENDING_FINANCE→APPROVED (my createOffer→V submitForApproval/smApprove/financeApprove). createOffer now creates DRAFT + delegates to V's lib/approval (single source). V also fixed run.sh local-db via docker-compose.dev.yml.
- [2026-06-13T13:00:00Z] Owner — 23-feature AUDIT (6-agent workflow, verified vs real code): P0 8 done + 2 partial, P1 5 done + 2 partial, P2 0/6 (intentional), HERO 3/3 REAL. Then FIXED the demo-blockers it found: (1) role-switch redirected to / hiding the Rep dashboard + AI intake HERO -> now dashboardPathForRole(role) + added "My desk" Rep nav link (touches V's role-switch/page.tsx + nav.tsx); (2) notifications bell was a dead icon -> new /notifications inbox (listNotifications + mark-read + click-through to record) + bell is now a Link; (3) deal notes were missing -> addDealNote action + notes UI on /deals/[id]. All tsc clean, pages 200. Now: P0 10/10 done, P1 6/7 (only #11 free-text search still partial). Production `next build` verified locally (peak ~880MB).
- [2026-06-13T14:45:00Z] V — P1 #11 GLOBAL SEARCH done → closes the last P1 gap (P1 now 7/7). New lib/search.ts globalSearch(q) = case-insensitive substring across account name/region/segment/industry, deal name/notes, case title/description, contact name/email/title (10 each) + new /search results page (grouped, links to /accounts/[id] /deals/[id] /cases/[id]) + a search box wired into the shared nav (V-owned). Universal (no role gate). Test: tsc clean + next build GREEN (/search compiled). Pushed to main.
- [2026-06-13T13:30:00Z] Owner — DEPLOYED LIVE to Frankfurt. URL http://43.165.2.182:3000 reachable externally (200). docker compose project hmd-crm (isolated from jianglai), app:3000, postgres internal+healthy, entrypoint auto db-push+seed+start. Added 4GB temp swap (/swapfile2) — build peaked fine. LIVE AI intake verified source:ai via Featherless on the server. All roles 200 (/rep /manager /finance /notifications /accounts). Redeploy = ssh frankfurt 'cd /opt/hmd-crm && git pull && docker compose up -d --build'. NOTE: V's /search (d6a64bb) landed AFTER this deploy — redeploy needed to include it live.
- [2026-06-13T15:30:00Z] Owner — P2 (4 of 6, user-approved) DONE + live + redeployed. #23 AI forecast narrative (Finance+Manager "Pipeline health", Featherless+fallback). #22 AI case summary (>=5 notes, seeded 2 cases w/ threads). #19 CSV export (/api/export/forecast + /cases, buttons on Finance). #18 SLA due-date (Case.dueDate seeded by priority; Overdue/Due-soon badges on TAM + case detail). Schema change (dueDate) applied live via entrypoint db push. #20 email-to-case + #21 Outlook NOT done (no Microsoft Graph/Entra access). Full prod build green; all verified on http://43.165.2.182:3000. SCORE NOW: P0 10/10, P1 7/7, P2 4/6, HERO 3/3.
- [2026-06-13T17:45:00Z] V — rubric self-assessment + P1a chips. (1) Self-assessed vs the 4 rubric criteria → memory/rubric-scorecard.md: P0 PASS(gate), 8-step demo STRONG-but-unverified-by-human (TOP risk — V doing the human walk-through now), seed STRONG, AI-native STRONG. Corrected a false alarm: an apparent "main compile fail" was just my STALE LOCAL Prisma client — `prisma generate` → tsc + next build GREEN; Owner's schema Case.dueDate is correct, Frankfurt rebuild is safe. (2) Built the missing **P1a 3 preset query chips**: /views + lib/views.ts (at-risk DACH enterprise / offers pending Finance / cases blocking customer tests) + nav "Smart views". tsc clean + next build GREEN. NOTE for Owner: /views needs a Frankfurt redeploy to appear live; it's NOT on the 7-step demo path so the live walk-through is unaffected.
- [2026-06-13T19:29:00Z] V — HUMAN DEMO WALK-THROUGH on live (http://43.165.2.182:3000) = **9/9 steps PASS**. Clicked through as an untrained user: Rep dashboard + AI intake (paste→draft→Apply→new account) + NBA panel + offer build (discount-needs-justification enforced) + SM→Finance approval chain (lock + Finance-after-SM correct) + TAM add-note/close + Manager stalled+weighted pipeline + Finance 3-yr device/service split + reseller-hides-Contract-negotiation + search. **Closes the rubric TOP risk — the 8-step demo is now human-verified.** ONE non-bug observation: adding a note to a case with ≥5 notes feels ~1.5s slow — it's the AI case summary (P2 #22) regenerating via Featherless on re-render; the note itself saves instantly (addCaseNote = pure DB writes, no AI). Verdict: NOT fixing (works; only on long-thread cases; the delay is the LLM working). Demo tip: use a short-thread case for the add-note beat. Also landed build-green but **NOT yet live**: P1a chips /views (68af7c3) + role-switch cache-bust revalidatePath("/","layout") (6b1d8cd, hardens the role-switch-heavy demo). **ACTION for Owner → REDEPLOY to ship these live: `ssh frankfurt 'cd /opt/hmd-crm && git pull && docker compose up -d --build'` (it re-seeds; do the final redeploy before recording).**
- [2026-06-13T19:35:00Z] V — REDEPLOY DONE (V has frankfurt SSH). Pulled `5c256c3`→`3eb5293` on the server + `docker compose up -d --build`. NOW LIVE: P1a chips `/views` + role-switch cache-bust + everything to HEAD. Verified: all routes 200 (`/ /views /role-switch /tam /finance /approvals`), fresh seed loaded (12 cases / 5 offers / 6 approvals / 6 notifications), server HEAD=`3eb5293`, jianglai untouched, Next.js "Ready in 461ms". Minor non-issue: entrypoint uses `next start` → warns about `output:standalone` (works fine, served 200; optional lower-RAM switch = `node .next/standalone/server.js`, Owner's domain). Owner: NO redeploy needed now — just do ONE final `git pull && docker compose up -d --build` right before recording for a clean seed.
- [2026-06-13T16:10:00Z] Owner — HMD-FEEDBACK enhancement live: Account gained domain/address/vatId; Contact gained decisionRole enum (FINANCIAL/BUDGET/TECH/INFLUENCER/OTHER, derived from title in seed). Shown on Account 360 (header customer-basics line + contact decision-role badge + phone). Direct from HMD provider chat (they explicitly asked for customer name/domain/address/VAT + contact decision-maker role). Schema additive; live-verified on Frankfurt. NOT done (provider "idea", needs external news/web data): AI news-based prospecting (Nokia-sold-network → suggest contact) — recorded as pitch roadmap/future-work, not built.
- [2026-06-13T20:07:00Z] V — GM (gross margin) alignment to HMD's "example columns" pipeline xlsx (from organizers). Added Product.gmPercent + Service.gmPercent (additive defaults; seeded realistic: devices 30-40%, accessories 45-70%, internal services 55-72%, third-party services 28-30%). Catalog shows per-item GM%. Finance 3-yr forecast now shows Net sales + Gross margin + GM% (KPI cards + per-quarter GM column); aggregated forecast GM uses a blended device 35% / service 55% assumption (lib/forecast DEVICE_GM_PCT/SERVICE_GM_PCT, documented like stage probs). Finance subtitle maps our funnel to HMD's Opportunity→Pipeline→Committed→Confirmed. tsc + next build GREEN. NEEDS redeploy (schema change → entrypoint db push adds gmPercent + reseed).
- [2026-06-13T17:00:00Z] Owner — CLAIMING audit-gap fixes (user said fix all). Owner taking: C1 create-case-from-account, M5 seed case-activity timeline, M3 case reassignment, M4 committed/at-risk/gap-to-target, C2 service-invoicing-models, dashboard role guards. V: please AVOID editing prisma/seed.ts case section, lib/forecast.ts service-revenue, lib/reporting.ts forecast, app/cases/*, app/manager/* until Owner lands these (~next hour) — ping to coordinate. Owner will rebase frequently.
- [2026-06-13T17:45:00Z] Owner — ALL 6 AUDIT GAPS FIXED + live + redeployed (a33ed46). C1 create-case-from-account (createCase + /cases/new + Account 360 button). M5 seed case-activity timeline (CASE_OPENED/NOTE_ADDED/CLOSED + internal-tier note). M3 case reassignment (reassignCase + case-detail select). C2 service-invoicing models now drive DIFFERENT revenue curves (Deal.serviceModel reshape: ONE_OFF=point/FIXED_TERM=spread/MONTHLY=device-trajectory — verified 1200,0,0,0 vs 300×4 vs 120/240/360/480; brief 2.2 no longer flattened). M4 committed/at-risk/gap-to-target (lib/targets.ts + Manager card, SM #4). Dashboard role guards (Rep/TAM bounced off /manager+/finance). tsc clean, all live on http://43.165.2.182:3000. Brief coverage now: personas + business-rule logic fully addressed; remaining are MINORs (post-create deal edit, finance period/stage/size filters).
- [2026-06-13T22:05:00Z] PLAN: polish UI WITHOUT touching backend/features. V = visual restyle via Lovable references (theme tokens + shared ui primitives + nav/layout + page polish) — keep all <form action>/<input name>/data props/server actions intact (see docs/UI-RESTYLE-GUIDE.md). Owner = build floating AI chat assistant (conversational query + usage help, Featherless + data, mounted in app/layout.tsx) + forecast CHART (Finance/Manager) — both using shared primitives so they inherit V's restyle. ~11h to freeze (09:00Z). Keep the current deployed app as the locked fallback; nothing gets deleted.
- [2026-06-13T23:00:00Z] Owner — DEMO UPGRADES live + redeployed (d5ae523). (1) "Aino" floating AI assistant (bottom-right): conversational query over LIVE role-aware data + usage help; lib/ai/assistant.ts + /api/ai/assistant + components/ai-assistant.tsx mounted in layout; Featherless + keyword fallback; verified source:ai naming real deals/numbers. (2) 3-year forecast CHART (dependency-free SVG, device/service stacked + stage-weighted line) on Finance + Manager — fixes the "table-only looks unfinished" risk. Both use shared primitives/theme tokens so V's Lovable restyle flows in. NOT done (optional, offered): 7-stage funnel viz (manager already has stage bars), "Aino" rename of existing AI badges (collision risk w/ V's restyle), Y2/Y3 'projected' label, SLA-breach seed notification, AI offer drafter. Current app stays the locked fallback.
- [2026-06-13T23:30:00Z] Owner — live + redeployed (67b2000): (1) 7-stage pipeline FUNNEL on Manager (SVG, width∝deal count, opacity∝win-signal, flags Contract-negotiation as direct-only + reseller note). (2) Aino assistant now DEEPLY per-user context-aware: REP gets their account NAMES + stage mix + win/loss + at-risk w/ idle days; SM gets team rep names + open counts + stalled/forecast. Verified live source:ai naming Sofia's real accounts + Mira's team. Demo upgrades complete: Aino assistant, forecast chart, pipeline funnel — all on shared primitives so V's Lovable restyle flows in.
- [2026-06-14T00:00:00Z] Owner — Aino upgraded to a proactive personal assistant (live, 56fc8e9): on open it GREETS BY NAME with a situational line ("Hi Sofia — you have 1 at-risk deal and 2 offers in approval...") via GET /api/ai/assistant + assistantGreeting(); shows DATA-DRIVEN WORK chips (not how-to) tied to real records — "Follow up: Aurora ward expansion", "Unstick: RheinWerk warehouse scanners", "Tackle: <worst case>", "Plan my day" (per role). askAino system prompt now leads with WHAT-TO-DO + prioritised 3-item to-do when asked to plan; how-to only on explicit ask. Verified per role (REP/SM/FINANCE/TAM) live.
- [2026-06-14T00:05:00Z] V — PREMIUM UI RESKIN ported from V's Lovable "NextGen CRM Canvas" (styling only, ZERO wiring changes per UI-RESTYLE-GUIDE). app/globals.css = violet/indigo oklch token system (stored as CHANNELS + wrapped oklch(var/<alpha-value>) so every /opacity utility — bg-primary/90, border-primary/40, bg-muted/50 — keeps working) + Sora/Manrope/JetBrains fonts + glass-card / ai-gradient / ai-gradient-text / aurora / shimmer classes. tailwind.config rewritten to the new tokens + success/warning/info/sidebar/chart colors + font families. Restyled shared primitives (Card→glass-card, Button depth, Badge semantic tones, Table uppercase headers) + nav (gradient shield logo, Sora wordmark, glass header, initials avatar) — so EVERY page inherits the look with no page edits. Lovable Vite source kept as LOCAL reference in design/lovable-canvas (gitignored + tsconfig-excluded). Owner's forecast-chart/pipeline-funnel unaffected (they use var(--chart-*, #hex fallback)). tsc + next build GREEN. NEEDS redeploy to view live.
- [2026-06-14T01:17:00Z] V — UI v2 MIGRATION (Lovable nextjs-export) LIVE on Frankfurt. Whole app switched to the new SIDEBAR AppShell (components/canvas/AppShell, role-aware nav to our real routes). 13 V-owned back-office pages ported to the canvas *Screen components: /finance (reference) + /manager /tam /cases/[id] /cases/new /approvals /approvals/[offerId] /catalog /views /reports /search /notifications /role-switch — each = server page fetches our Prisma data → maps to the screen's *Data type → renders <Screen/>; **ALL forms/actions kept wired** (where a screen had no slot — e.g. case add-note/close/escalate, catalog edit/retire, manager reassign, approval decision — our wired form is KEPT restyled below the screen, functionality never dropped). New UI namespaced under components/canvas/ + lib/canvas/ (oklch tokens already matched, zero conflict). tsc + next build GREEN (24 routes); redeployed + verified (sidebar shipped, screens render with seed data). **DELIBERATELY NOT ported (protecting the AI HEROES): Owner's /rep (paste→preview→Apply intake), /accounts/[id] (NBA), /offers/*, /deals/* — porting would break the client-side AI flows. They already have the new shell + token look. COORDINATE WITH OWNER before touching those.** Lovable source: NextGen CRM Canvas.zip (root, committed) + design/lovable-canvas (gitignored).
- [2026-06-14T00:30:00Z] Owner — AUTONOMOUS OVERNIGHT SESSION (user asleep, no input until done; target ~4-5h, no pausing). PLAN: (A) fix all DEGRADED canvas regressions [Finance AI-narrative+filters+dead-select, Case-detail activity-timeline+de-dup-forms, TAM per-row badges, Catalog de-dup, Manager past-close owner]; (B) port the 6 AI-hero pages to canvas screens (rep/account360/deals-new+detail/offers-new+detail) keeping all AI+form wiring — also fixes the <main> double-wrap; (C) full verification (8-step demo path + all audit fixes + AI features + build green + every route 200); (D) deploy the completed canvas version to Frankfurt + verify live, OR keep pre-canvas live as fallback if blocked. Version mgmt: commit per unit, push frequently, main always builds, tag checkpoints. Live URL is the rollback.
- [2026-06-14T03:09:35Z] Owner — AUTONOMOUS OVERNIGHT SESSION DONE (commits c8c69bd..17eea72 on main, pushed). Answers V's "coordinate before touching the AI-hero pages" note: ported the 4 hero pages to canvas screens WITH every AI/form hero PRESERVED via a slot pattern (pass the real interactive component into the screen as a ReactNode/action prop, never re-implement it): /rep mounts the real <IntakePanel/> (email→draft→Apply) via intakeSlot + derives NBAs from real signals; /accounts/[id] mounts the streamed <NbaPanel/> (Suspense) via nbaSlot + restores add-note; /deals/[id] = DealDetailScreen (forecast chart hero) with data-driven REAL stage enum + move-stage/add-note adapter actions; /offers/[id] = OfferDetailScreen proposal view (unwired send/accept forms HIDDEN, no placeholders). DEGRADED all fixed: Finance restored AI pipeline-health narrative + real channel/owner filters (dropped the dead period select); catalog DE-DUP (was rendering CatalogScreen tables AND a Manage block = everything twice → single editable table + inline Add rows); TAM per-row status/SLA/service badges (match SLA-overdue KPI); manager past-close shows deal owner. Also: root / now redirects (cookie check) to role-switch / role dashboard (was the old WAVE-0 overview); dropped redundant nested <main> landmarks. VERIFIED: next build GREEN (25 routes) + all routes 200 by role; AI heroes all resolve LIVE (intake, NBA, forecast narrative, case summary, Ask Aino + greeting/actions); CSV exports + approvals queue/detail + search + notifications all work; case add-note mutation confirmed end-to-end via the canvas form→adapter→addCaseNote chain. **NOT DEPLOYED — autonomous prod/ssh access was (correctly) blocked; live http://43.165.2.182:3000 still runs the pre-session build. ACTION → redeploy to ship: ssh frankfurt 'cd /opt/hmd-crm && git pull && docker compose up -d --build' (watch build-OOM on ~0.5Gi free RAM — V's /swapfile2 should cover it).**
- [2026-06-14T08:28:56Z] Owner — REDEPLOYED LIVE (owner-authorized). Frankfurt /opt/hmd-crm pulled to HEAD 4cd3f6b + `docker compose up -d --build` (ran in detached tmux; old container served 200 throughout = zero downtime; ~1.9Gi RAM free after, build OK on existing swap). NOW LIVE on http://43.165.2.182:3000 = the full overnight session (canvas hero ports w/ AI heroes preserved + all degraded fixes + root redirect). VERIFIED live: root 307→/role-switch (new build), /rep /tam /manager /finance /catalog + account-360/deal-detail all 200; Finance AI pipeline-health narrative + channel filter present; catalog single table (de-dup); TAM per-row SLA badges; Ask Aino source:ai. Co-tenant jianglai UNTOUCHED (3 containers up 2 months). Fresh seed loaded. Redeploy = ssh frankfurt 'cd /opt/hmd-crm && git pull && docker compose up -d --build'.
- [2026-06-14T08:56:20Z] Owner — REDEPLOYED (owner-authorized) HEAD 4b685de live on http://43.165.2.182:3000. Two AI fixes shipped after a multi-agent demo-readiness audit (18/23 features done, demo path smooth, no blocker): (1) the dead 'Daily brief' button on /rep now opens Aino (personalised AI brief); (2) NEW real 'Generate with AI' on the offer discount-justification field (demo step 8 / approval path) — /api/ai/offer-justification (aiText + fallback) + button in offer-builder (shows when discount>0). Verified live: Daily-brief present, justification API source:ai, zero downtime, jianglai untouched. SCRIPT NOTES for demo (audit): step 3 say 'Build offer' not 'open catalog as Rep' (catalog is Finance-only); to show 3 service-revenue curves create a deal live (seed defaults all to MONTHLY_RECURRING); don't claim edit-account/contact or offer version-history.
