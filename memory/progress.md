# Build Progress Log — append-only

> One line per meaningful slice landing or status change: `- [UTC time] who — slice — state — test result`.
> Append-only; never rewrite past lines. Run `./scripts/sync.sh` after appending.
> States: started | branch-green | merged-to-main | blocked. Detailed task states live in team-board.md.

- [2026-06-13T10:15:00Z] Plan — parallel build plan written (plan-owner.md, plan-v.md, parallel-plan.md). Streams: V=foundation+data+back-office+forecast-engine, Owner=sales-front+AI+server/deploy. Awaiting V WAVE 0 foundation.
- [2026-06-13T11:00:00Z] Owner — server prep DONE. Frankfurt has Docker 29.3.1+Compose v5.1.1+git; /opt/hmd-crm created; ports 3000/5432 free. WARNING: shared box with live "jianglai" project (don't touch; port 80/8000/3307 taken). RAM tight (~0.5Gi free +2.1Gi swap) → build-OOM risk. Constraints written to memory/deploy-notes.md for V's docker-compose (project name hmd-crm, app:3000, postgres internal, standalone build).
- [2026-06-13T11:35:00Z] Owner — Featherless smoke test PASSED (key from PromptFinance/.env). Qwen/Qwen2.5-7B-Instruct = 200, ~1.84s, clean JSON in content → KEPT as default chat model. GLM-4.6 = 200 but returns answer in `reasoning` field w/ empty content (reasoning model) → avoid for JSON; client now reads content||reasoning as a safety net. AI provider fully validated; only TODO = copy FEATHERLESS_API_KEY into our repo .env at deploy.
- [2026-06-13T11:36:00Z] V — WAVE 0 foundation — merged-to-main (fb3aaab) — Next.js 15 scaffold + full Prisma schema (14 entities) + rich seed (8 accounts/18 deals/3-yr forecast/12 cases/full catalog/offers in every approval state) + docker-compose/Dockerfile/run.sh + demo role-switch shell + lib/* helpers. Test: tsc --noEmit clean + next build GREEN; runtime seed verify pending local Postgres role provisioning. **Owner UNBLOCKED — branch off main now.** Integration contract = lib/session(currentUser/currentRole), lib/activity(createActivityEvent), lib/notify(notify), lib/forecast(weightedRevenue + quarter aggregation). NOTE for deploy: docker-compose still exposes postgres:5432 + uses Azure OpenAI env names — needs aligning to deploy-notes (postgres internal, project name hmd-crm) + Featherless env before Frankfurt.
- [2026-06-13T11:42:00Z] V — foundation deploy-align — merged-to-main — RESOLVED the deploy NOTE above: .env.example now Featherless (FEATHERLESS_API_KEY / FEATHERLESS_MODEL=Qwen/Qwen2.5-7B-Instruct / FEATHERLESS_BASE_URL, dropped AZURE_OPENAI; added POSTGRES_*); docker-compose.yml per deploy-notes (project name hmd-crm, app:3000, postgres INTERNAL/not published, postgres:16-alpine, env-driven, app DATABASE_URL→host "db"). lib/* helper signatures reconfirmed == parallel-plan. tsc + next build still GREEN. RUNTIME (docker compose up → seed + render) NOT run locally — Docker is NOT installed on V's machine (WSL2 is present); container verify deferred to Frankfurt (has Docker 29.3.1 + Compose) or a local Docker install. Owner remains unblocked.
- [2026-06-13T12:08:00Z] V — WAVE 1 ALL FOUR SLICES landed on main — merged-to-main — built in parallel (4 subagents): SA-V1 Catalog (/catalog + lib/catalog.ts, retire logic, activeProducts/Services feed for Owner's offer builder), SA-V2 Offer approval (/approvals + /approvals/[offerId] + lib/approval.ts STATE MACHINE = submitForApproval/smApprove/smReject/financeApprove/financeReject — discount-needs-justification, lock-while-pending, HARD Finance-after-SM guard, reject-unlocks, $transaction, notify+activity on every step), SA-V3 TAM cases (/tam + /cases/[id] + lib/cases.ts — notes w/ internal tier, close sets closedAt, escalate), SA-V4 Manager+Finance+forecast (/manager + /finance + /reports + lib/reporting.ts — 3-yr quarterly forecast device/service SEPARATE, weighted pipeline, stalled>14d, past-close, quarter/half/year toggle, reassign, close rate). nav.tsx wired with role-aware links. Test: tsc --noEmit clean + next build GREEN (all 11 routes compiled, dynamic). lib/approval.ts hand-reviewed = correct. **Contracts for Owner:** offer builder calls lib/approval.submitForApproval(offerId) + uses lib/catalog.activeProducts()/activeServices(). RUNTIME seed+click verify still pending Docker (Frankfurt). main stays build-demoable.
- [2026-06-13T12:30:00Z] Owner — RUNTIME FOUNDATION VERIFIED (the part V couldn't): prisma db push OK, seed wrote 8 accounts/18 deals/156 forecast rows/12 cases/5 offers OK, dev server renders /, /role-switch 200. Featherless key validated live (Qwen2.5-7B ~1.8s).
- [2026-06-13T12:30:00Z] Owner — SA-O1 DONE & merged to main (ab46225): Account 360 page (/accounts/[id]: deals+cases together, offers, timeline, notes+add-note action, contacts, NBA panel) + Rep dashboard (/rep: my accounts, at-risk deals, pipeline by stage, offers in approval, activity) + rule-based Next Best Action panel (live now; SA-O4 adds LLM on top). tsc clean, both pages 200 with seed data.
- [2026-06-13T12:30:00Z] Owner -> V FINDING: run.sh local path is broken — docker-compose `db` uses `expose` (internal only, NOT published to host), so `npm run dev` on host can't reach localhost:5432. For LOCAL dev the db must be published (e.g. a docker-compose.override.yml mapping 5432, or run.sh starts a published dev db). Frankfurt deploy is fine (full app-in-compose reaches db by service name). I verified using a standalone published postgres. Please fix the local-dev path, V.
- [2026-06-13T13:00:00Z] Owner — SA-O2 DONE (main): /deals/new (channel→stage live filter, reseller hides Contract negotiation, 4-quarter year-1 forecast input) + createDeal action (writes 12 DealForecastPeriod rows: y1 entered, y2/y3 projected ×1.2/1.35, device/service separate, weighted by stage prob) + /deals/[id] detail (3-yr forecast table, move-stage re-weights rows). tsc clean; pages 200; nested deal+12-row write verified + cascade cleanup. Account "New deal" button now works.
- [2026-06-13T13:30:00Z] Owner — SA-O3 DONE (main): /offers/new offer builder (active catalog products+services, qty, live total, discount% + justification REQUIRED when >0) + createOffer action + /offers/[id] detail (line items w/ price snapshots, discount+justification, status, approval history). Verified: discounted submit -> Offer.status=PENDING_SM, locked, Approval{SALES_MANAGER,PENDING}, all SMs notified; no-discount submit -> APPROVED. tsc clean, pages 200, write path + cascade verified.
- [2026-06-13T13:30:00Z] HANDOFF CONTRACT for V (SA-V2 approval queues) — Owner's submit leaves a discounted offer at: Offer.status=PENDING_SM, locked=true, one Approval{step:SALES_MANAGER,status:PENDING}, SMs notified. V's queue actions continue: SM approve -> Approval(SM)=APPROVED + Offer.status=PENDING_FINANCE + create Approval{FINANCE,PENDING} + notify FINANCE; Finance approve -> Approval(FINANCE)=APPROVED + Offer.status=APPROVED + notify rep; reject(any) -> Approval=REJECTED + Offer.status=REJECTED + locked=false + notify rep. (Full contract in app/offers/actions.ts header.)
- [2026-06-13T13:35:00Z] V — FIX for Owner's local-dev finding + WAVE 1 verified on main. (1) Added docker-compose.dev.yml (PUBLISHED postgres on host:5432); run.sh/run.ps1 now start it before `next dev`. Main docker-compose.yml stays postgres-INTERNAL (Frankfurt full-stack unaffected). Host `next dev` -> localhost:5432 works again. (2) WAVE 1 SA-V1..V4 merged to main; COMBINED tree (Owner SA-O1/O2/O3 + V WAVE 1 = 17 routes) passes tsc + next build GREEN. Owner: approval queues are live at /approvals — your offer submit already lands PENDING_SM + notifies SM, so SM->Finance approve/reject now works end-to-end via lib/approval.ts.
- [2026-06-13T14:30:00Z] Owner — SA-O4 core DONE (main): lib/ai/{client,intake,nba} on Featherless (runtime env read; exact-shape prompt; content||reasoning). AI-assisted intake (rep dashboard: paste→/api/ai/intake→draft preview→applyIntake writes account+contact+deal+case+note) VERIFIED source:ai (extracted contact+deal+CRITICAL case+task). NBA panel on account page via LLM+rules fallback, Suspense-streamed (page paints instant). Removed debug diag route.
- [2026-06-13T14:30:00Z] INTEGRATION VERIFIED — full app (Owner SA-O1..O4 + V SA-V2..V4) tsc CLEAN; all 14 routes 200 by role (/rep /accounts /deals /offers + /tam /manager /finance /approvals /catalog /reports). Offer workflow cross-person chain tested end-to-end: DRAFT→PENDING_SM→PENDING_FINANCE→APPROVED (my createOffer→V submitForApproval/smApprove/financeApprove). createOffer now creates DRAFT + delegates to V's lib/approval (single source). V also fixed run.sh local-db via docker-compose.dev.yml.
- [2026-06-13T13:00:00Z] Owner — 23-feature AUDIT (6-agent workflow, verified vs real code): P0 8 done + 2 partial, P1 5 done + 2 partial, P2 0/6 (intentional), HERO 3/3 REAL. Then FIXED the demo-blockers it found: (1) role-switch redirected to / hiding the Rep dashboard + AI intake HERO -> now dashboardPathForRole(role) + added "My desk" Rep nav link (touches V's role-switch/page.tsx + nav.tsx); (2) notifications bell was a dead icon -> new /notifications inbox (listNotifications + mark-read + click-through to record) + bell is now a Link; (3) deal notes were missing -> addDealNote action + notes UI on /deals/[id]. All tsc clean, pages 200. Now: P0 10/10 done, P1 6/7 (only #11 free-text search still partial). Production `next build` verified locally (peak ~880MB).
- [2026-06-13T14:45:00Z] V — P1 #11 GLOBAL SEARCH done → closes the last P1 gap (P1 now 7/7). New lib/search.ts globalSearch(q) = case-insensitive substring across account name/region/segment/industry, deal name/notes, case title/description, contact name/email/title (10 each) + new /search results page (grouped, links to /accounts/[id] /deals/[id] /cases/[id]) + a search box wired into the shared nav (V-owned). Universal (no role gate). Test: tsc clean + next build GREEN (/search compiled). Pushed to main.
- [2026-06-13T13:30:00Z] Owner — DEPLOYED LIVE to Frankfurt. URL http://43.165.2.182:3000 reachable externally (200). docker compose project hmd-crm (isolated from jianglai), app:3000, postgres internal+healthy, entrypoint auto db-push+seed+start. Added 4GB temp swap (/swapfile2) — build peaked fine. LIVE AI intake verified source:ai via Featherless on the server. All roles 200 (/rep /manager /finance /notifications /accounts). Redeploy = ssh frankfurt 'cd /opt/hmd-crm && git pull && docker compose up -d --build'. NOTE: V's /search (d6a64bb) landed AFTER this deploy — redeploy needed to include it live.
- [2026-06-13T15:30:00Z] Owner — P2 (4 of 6, user-approved) DONE + live + redeployed. #23 AI forecast narrative (Finance+Manager "Pipeline health", Featherless+fallback). #22 AI case summary (>=5 notes, seeded 2 cases w/ threads). #19 CSV export (/api/export/forecast + /cases, buttons on Finance). #18 SLA due-date (Case.dueDate seeded by priority; Overdue/Due-soon badges on TAM + case detail). Schema change (dueDate) applied live via entrypoint db push. #20 email-to-case + #21 Outlook NOT done (no Microsoft Graph/Entra access). Full prod build green; all verified on http://43.165.2.182:3000. SCORE NOW: P0 10/10, P1 7/7, P2 4/6, HERO 3/3.
- [2026-06-13T17:45:00Z] V — rubric self-assessment + P1a chips. (1) Self-assessed vs the 4 rubric criteria → memory/rubric-scorecard.md: P0 PASS(gate), 8-step demo STRONG-but-unverified-by-human (TOP risk — V doing the human walk-through now), seed STRONG, AI-native STRONG. Corrected a false alarm: an apparent "main compile fail" was just my STALE LOCAL Prisma client — `prisma generate` → tsc + next build GREEN; Owner's schema Case.dueDate is correct, Frankfurt rebuild is safe. (2) Built the missing **P1a 3 preset query chips**: /views + lib/views.ts (at-risk DACH enterprise / offers pending Finance / cases blocking customer tests) + nav "Smart views". tsc clean + next build GREEN. NOTE for Owner: /views needs a Frankfurt redeploy to appear live; it's NOT on the 7-step demo path so the live walk-through is unaffected.
