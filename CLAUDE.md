# CLAUDE.md — Sales Hackathon 2026 — HMD

This is a hackathon event repo managed with hackathon-skills. Deadline (UTC): 2026-06-14T12:00:00Z.

## Mandatory for every agent session

1. Read `memory/HACKATHON.md` before any task.
2. Compute remaining time: `date -u +%Y-%m-%dT%H:%M:%SZ` vs `event.deadline_utc` in `memory/event.yaml`.
3. If inside the freeze window (deadline minus `event.freeze_hours` = 2026-06-14T09:00:00Z), refuse build work; only closer/demo/deck tasks are allowed. Route to `hackathon-closer`.
4. When the task is ambiguous, invoke `hackathon-router`.

## Facts live in memory/event.yaml

Before asking the user anything, check `memory/event.yaml`. Ask only missing fields, in one batch, then write answers back and commit. Never re-ask a recorded fact. Unknown answers are recorded as `TBD`, not stalled on.

## Scope rules

- `memory/pitch-spec.md` = narrative + demo path + 3 HERO contract. `/BUILD-SPEC.md` (by V) = canonical detailed spec (data model, business rules, 9-phase order, acceptance checklist). Keep both consistent.
- Scope = FULL BRIEF (team override of the generic 7-cap): all 10 P0 + all 7 P1 + P2 opportunistic. Discipline: build in BUILD-SPEC 9-phase order; the 7-step demo path must run end-to-end on `main` before any P2; AI after P0; prefer fewer working features over broken placeholders.
- 3 HERO differentiators (full polish): AI-assisted intake (email→draft→Apply, Phase 8), AI Next Best Action (Phase 8), 3-yr time-phased weighted forecast (Phase 7, device/service split).
- Fake/hardcoded data is legal. HMD brief REQUIRES realistic seed data — empty DB is penalized.

## Project specifics

- Challenge: build HMD Secure's AI-native CRM. Full spec in agent memory `hmd-crm-challenge`.
- Stack: Next.js 15 + TS + Tailwind + shadcn/ui · Postgres + Prisma · Auth.js (roles: Rep/TAM/SM/Finance) · Azure OpenAI.
- Deploy: Frankfurt server 43.165.2.182 via Docker Compose (use the `frankfurt` skill). EU region = satisfies data-residency constraint.
- The 8-step demo line IS the rubric — keep `main` able to walk it.

## Git rules

- `main` is always demoable. Run `./run.sh` before merging; if it fails, do not merge.
- Personal branches: `feat/<name>-<topic>`, lifetime ≤2 hours.
- `memory/` edits happen on main only; after editing run `scripts/sync.sh` immediately.
- `memory/decisions.md` is append-only. `memory/HACKATHON.md` is regenerated by skills, never hand-edited.
- Personal agent scratch goes in `.agent/` (gitignored). Only conclusions enter `memory/`.

## File layout

- `app/core/` — demo critical-path code (P0). `app/glue/` — supporting code. Keep them physically separate so late-stage cuts are fast.
- `demo/` — demo script, recording checklist, deck sources.
- `memory/` — team state, git-synced.
